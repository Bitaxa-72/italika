Работа с репозиторием:

- `old_website/` - архив старого сайта. Без прямого указания не изменять, не форматировать, не чистить и не переносить файлы внутри этой директории. Допустимо только читать ее как источник данных для миграции.
- Не переделывать существующую архитектуру, тему, шаблоны и экспортные скрипты без явного запроса. Сначала разобраться в текущем поведении и делать минимальные точечные изменения под задачу.
